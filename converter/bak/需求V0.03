自动化规则转换与 Git 同步工具 (Rule-Converter) 需求文档
版本	日期	核心变更	状态
V0.01	2026-01-31	基础转换逻辑（Gemini/OpenAI）	已归档
V0.02	2026-01-31	增加配置文件、时间戳预检、保留注释	已归档
V0.03	2026-01-31	集成 Git 自动同步、支持多级目录架构	当前版本
1. 项目概述

本项目旨在自动化获取 GitHub 上的代理规则文件（.list），将其转换为特定格式的 .cvc 文件（带 payload: 标识），并自动将更新后的结果同步至指定的 Git 仓库。
2. 目录架构规范

程序需支持 Windows 环境下的多级路径管理：

    Root Dir (根目录): 存放所有 Git 仓库的基础路径（如 D:\MyRules）。

    Project (项目/仓库): 对应 GitHub 上的仓库名称（如 CmccBannedList）。

    Sub-Dir (输出子目录): 项目内部的特定分类目录（如 AI）。

    完整输出路径: {Root Dir}\{Project}\{Sub-Dir}。

3. 业务逻辑需求
3.1 预检与增量更新

    HTTP 预检: 使用 HEAD 请求获取远程文件的 ETag 或 Last-Modified。

    对比机制: 将标识符记录在 config.json 的 state 模块中。若远程标识符与本地一致，且本地目标文件存在，则跳过后续所有处理流程。

3.2 文件转换规则

    源文件 (LIST):

        动作: 镜像备份。

        要求: 不做任何修改，完整保留原始格式。

    目标文件 (CVC):

        首行: 固定为 payload:。

        注释行: 保留所有以 # 开头的行，并将其前置增加 2 个空格缩进，以符合 YAML 兼容格式。

        规则行:

            仅处理前缀为 DOMAIN,、DOMAIN-KEYWORD,、DOMAIN-SUFFIX, 的行。

            转换为 - 原始内容 格式。

            执行严格保序去重：同一规则多次出现时，仅保留第一次出现的行。

        空行: 丢弃所有纯空行。

3.3 Git 自动化同步

    前置动作: 转换前自动执行 git pull，确保本地仓库与远程同步。

    后置动作:

        若检测到文件有更新，依次执行 git add .、git commit（带时间戳备注）。

        根据配置决定是否执行 git push 上传至远程仓库。

    认证要求: 依赖 Windows 凭据管理器存储的 Token 或本地 SSH Key，实现无交互静默推送。

4. 配置文件规范 (config.json)

采用 JSON 格式，分为 settings（静态配置）和 state（动态状态）两部分：
JSON

{
    "settings": {
        "root_dir": "根目录路径",
        "project_name": "仓库名",
        "sub_dir": "子目录名",
        "git_integration": {
            "enabled": true,
            "auto_pull": true,
            "auto_push": true,
            "commit_message": "提交备注前缀",
            "remote_name": "origin",
            "branch": "main"
        }
    },
    "state": {
        "任务名": "上一次的ETag标识符"
    }
}

5. 环境与运行要求

    运行环境: Python 3.10+（推荐 Python 3.14+），Windows 10/11。

    依赖项: 仅使用标准库（os, json, urllib, subprocess, tempfile）。

    外部依赖: 系统需安装 Git 客户端，并将其路径添加至系统环境变量 Path。

6. 核心流程图

这份需求文档已经整理完毕。如果你将来需要将这个工具扩展为支持更多的规则源（比如广告拦截、视频黑名单等），只需要在代码的 tasks 列表和 config.json 的 state 中增加对应的条目即可。